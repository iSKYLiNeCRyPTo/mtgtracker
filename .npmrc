# Skip optional native addons (e.g. sharp inside @xenova/transformers).
# The web app uses @xenova/transformers via WASM — sharp is only needed
# for the local pipeline. Install it separately: npm install --include=optional
omit=optional
