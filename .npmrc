# Prevent sharp's postinstall from downloading libvips from GitHub
# (blocked in Cloudflare's build network). Rollup/rollup platform
# binaries are plain files with no scripts, so they still install fine.
ignore-scripts=true
